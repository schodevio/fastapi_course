```bash
uv init
uv add "fastapi[standard]"
```

```bash
uvicorn books:app --reload # or
fastapi run books.py # or
fastapi dev
```

```bash
sqlite3 todos.db

.schema
.mode column
.mode markdown
.mode table
.mode box
.quit
```
